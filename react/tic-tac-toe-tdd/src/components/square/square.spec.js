import {render, screen} from '@testing-library/react';

describe("Squares", () => {
    describe("Clicking squares", () => {
        async function clickSquare() {
            render(<Square />)
        }
    })
})
